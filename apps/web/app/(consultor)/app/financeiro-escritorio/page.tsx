import { EmBreve } from '@/components/em-breve';

export default function FinanceiroEscritorio() {
  return (
    <EmBreve
      titulo="Financeiro do escritório"
      descricao="Cobrança dos clientes e despesas do próprio escritório — diferente do financeiro do produtor, que é isolado e não passa por aqui."
      fase="Sem especificação suficiente ainda — item de menu reservado. Ver docs/UX_ARCHITECTURE.md §1.1."
    />
  );
}
